namespace MagicalCompetition.Core.Model
{
    /// <summary>カードの色（属性）を表す列挙型。</summary>
    public enum CardColor
    {
        Fire,   // 火（赤）
        Water,  // 水（青）
        Light,  // 光（黄）
        Earth,  // 土（緑）
        Wind,   // 風（紫）
        Any     // 任意（「好きな色の5」疑似場札用）
    }
}
