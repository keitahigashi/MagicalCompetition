# TASK-0003: 列挙型（CardColor, GamePhase, PlayType）・Card クラス実装

**タスクID**: TASK-0003
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 1 - 基盤・データモデル構築
**信頼性レベル**: 🔵 *interfaces.md 列挙型セクション*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/magical-competition/requirements.md) - REQ-001, REQ-101~103
- **設計文書**: [📐 architecture.md](../../design/magical-competition/architecture.md)
- **型定義**: [📝 interfaces.md](../../design/magical-competition/interfaces.md)

## タスク概要

ゲームの基盤となる列挙型3種（CardColor, GamePhase, PlayType）とCardクラスの実装を行う。全てUnity非依存の純粋C#クラスとして実装する。

## 依存タスク

- **前提タスク**: TASK-0001
- **後続タスク**: TASK-0004, TASK-0005, TASK-0006, TASK-0008, TASK-0010

## 完了条件

- [ ] CardColor enumが6値（Fire, Water, Light, Earth, Wind, Any）で定義されている
- [ ] GamePhase enumが8値で定義されている
- [ ] PlayType enumが4値で定義されている
- [ ] Cardクラスが正しく動作する（生成・等値比較・ToString）
- [ ] 全テストケースがグリーンである

---

## 実装詳細

### 1. CardColor enum 🔵

**信頼性**: 🔵 *interfaces.md、マニュアルカード構成より*

```csharp
public enum CardColor
{
    Fire,
    Water,
    Light,
    Earth,
    Wind,
    Any
}
```

- `Any` は疑似場札用の特殊値

**実装ファイル**: `Assets/_Project/Scripts/Core/Model/CardColor.cs`

### 2. GamePhase enum 🔵

**信頼性**: 🔵 *interfaces.md、ユーザヒアリングより*

```csharp
public enum GamePhase
{
    Setup,
    SelectFieldCard,
    Play,
    Draw,
    CheckWin,
    NextTurn,
    AllPassReset,
    End
}
```

**実装ファイル**: `Assets/_Project/Scripts/Core/Model/GamePhase.cs`

### 3. PlayType enum 🔵

**信頼性**: 🔵 *interfaces.md、REQ-101~103より*

```csharp
public enum PlayType
{
    SameNumber,
    SameColor,
    Arithmetic,
    Pass
}
```

**実装ファイル**: `Assets/_Project/Scripts/Core/Model/PlayType.cs`

### 4. Card class 🔵

**信頼性**: 🔵 *interfaces.md Core/Modelより*

- Properties: `Color` (CardColor), `Number` (int 1-9)
- Constructor: `Card(CardColor color, int number)`
- `Equals`, `GetHashCode` オーバーライド（色と番号が一致すれば等値）
- `ToString`: `"Fire:5"` 形式で出力
- Unity非依存の純粋C#クラス

**実装ファイル**: `Assets/_Project/Scripts/Core/Model/Card.cs`

---

## 単体テスト要件

### 1. Card生成テスト 🔵

各色 × 各番号で正しく生成されることを確認する。

### 2. Card等値テスト 🔵

同じ色・番号のCardはEqualであることを確認する。

### 3. Card不等値テスト 🔵

異なる色または番号のCardはNotEqualであることを確認する。

### 4. Card.ToStringテスト 🔵

`"Fire:5"` 形式で出力されることを確認する。

### 5. CardColor.Anyテスト 🔵

疑似場札用の値 `Any` が存在することを確認する。

**テストファイル**: `Assets/_Project/Tests/EditMode/Core/Model/CardTests.cs`

---

## 実装手順

### TDD手順

1. **tdd-requirements**: interfaces.mdから列挙型・Cardクラスの仕様を確認する
2. **tdd-testcases**: 上記5つのテストケースをCardTests.csに記述する
3. **tdd-red**: テストを実行し、全てREDであることを確認する
4. **tdd-green**: 列挙型3種とCardクラスを実装し、全テストをGREENにする
5. **tdd-refactor**: 必要に応じてリファクタリングする
6. **tdd-verify-complete**: 全テストがパスし、完了条件を満たすことを確認する

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 4 | 0 | 0 | 4 |
| 単体テスト | 5 | 0 | 0 | 5 |

**品質評価**: ✅ 高品質
